return {
  {
    "rose-pine/neovim",
    name = "rose-pine",
    lazy = false,
    priority = 1000,
    opts = {},
  },
  -- add more colorschemes here (using `event = "VeryLazy"` and `lazy = true`)
}
